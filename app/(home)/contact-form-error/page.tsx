import { ContactError } from "@/components/contactError/contact-error";

const ContactFormError = () => {
    return(
        <div className="max-w-full">
            <ContactError/>
        </div>
    );
};

export default ContactFormError;